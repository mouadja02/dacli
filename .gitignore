# Secrets & environment
.env

# Virtual environments
.venv/
venv/

# Python build artifacts
*.pyc
__pycache__/
*.egg-info/
dist/
build/
.eggs/

# Test & coverage
*.log
.pytest_cache/
.coverage
htmlcov/

# Databases
*.db
*.sqlite3

# Local runtime state (generated, not committed)
config.yaml
config/connectors.yaml
src/dacli/config/connectors.yaml
config/policy.yaml
.dacli

# Rendered promo video (25 MB) — attach to the GitHub Release, don't bloat git
assets/dacli-promo.mp4

# Credential encryption key — NEVER commit. Covered by .dacli for the default
# location; this also catches a custom DACLI_STATE_PATH outside .dacli.
.key
**/.key

# /debug-connector backups of edited connector code
*.py.bak

# Local-only working dirs
test.py
.claude/
TEST.md
reporting/
.roadmap

# Local-only: agent guidance + the audit-derived prompt backlog. Never commit.
CLAUDE.md
roadmap/