# Secrets & environment
.env

# Virtual environments
.venv/
venv/

# Python build artifacts
*.pyc
__pycache__/
*.egg-info/
dist/
build/
.eggs/

# Test & coverage
*.log
.pytest_cache/
.coverage
htmlcov/

# Databases
*.db
*.sqlite3

# Local runtime state (generated, not committed)
config.yaml
config/connectors.yaml
src/dacli/config/connectors.yaml
.dacli

# Credential encryption key — NEVER commit. Covered by .dacli for the default
# location; this also catches a custom DACLI_STATE_PATH outside .dacli.
.key
**/.key

# /debug-connector backups of edited connector code
*.py.bak

# Local-only working dirs
test.py
roadmap/
.claude/
TEST.md
reporting/