# dacli

Your autonomous data engineering CLI agent. The assembled CLI: wires `dacli-core`,
`dacli-ai`, and `dacli-tui`, and ships the snowflake/github/shell seed extensions
plus the `register(api)` docs the agent reads to extend itself.

```
pip install dacli
dacli
```

Full docs: https://github.com/mouadja02/dacli
