# dacli reads the password from the environment; the rest (account, user,
# warehouse, database, role) you enter once in `dacli setup`, encrypted at rest.
# Power users who prefer a file: see docs/CONFIGURATION.md for the config.yaml
# `connectors.snowflake` block that references ${SNOWFLAKE_PASSWORD}.

LLM_API_KEY=
SNOWFLAKE_PASSWORD=
